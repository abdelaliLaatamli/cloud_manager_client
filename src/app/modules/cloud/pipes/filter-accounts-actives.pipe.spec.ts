import { FilterAccountsActivesPipe } from './filter-accounts-actives.pipe';

describe('FilterAccountsActivesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAccountsActivesPipe();
    expect(pipe).toBeTruthy();
  });
});
